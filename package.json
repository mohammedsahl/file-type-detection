{
    "scripts": {
        "trie": "tsc --downlevelIteration trie.ts && node trie.js && rm *.js",
        "start": "tsc read.ts && node read.js && rm *.js",
        "old":  "tsc fileType.ts && node fileType.js && rm *.js"
    }
}
